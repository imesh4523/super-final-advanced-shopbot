## Packages
recharts | Dashboard analytics charts
framer-motion | Smooth page transitions and dialog animations
date-fns | Date formatting for tables

## Notes
Authentication is handled via Replit Auth (useAuth hook).
API endpoints are defined in shared/routes.ts.
Dashboard needs to look premium with a clean, data-heavy layout.
